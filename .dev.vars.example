# ============================================================
# 本文件是「本地开发」用的示例，部署时不需要填任何东西。
#
# 注意：Cloudflare 的 Deploy 按钮会读取这个文件来生成部署表单，
# 所以这里**故意留空**——部署弹窗才不会要求你填选项。
#
# 所有配置都在部署完成后、于后台页面设置：
#   · 后台账号密码          → 首次访问 /admin 时创建
#   · 短信商密钥            → 后台「配置 → 短信通道凭证」
#   · Turnstile Secret      → 后台「配置 → 人机检测」
#   · 调试模式（不发真短信） → 后台「配置 → 运行模式」
#   · 允许的国家 / 站点来源  → 后台「配置」
#
# 本地开发时：把本文件复制为 .dev.vars，再按需填写，例如
#   MOCK_MODE=1
# 这样本地跑起来就不会真的发短信（验证码固定 123456）。
# .dev.vars 已被 .gitignore 排除，不会被提交。
# ============================================================
